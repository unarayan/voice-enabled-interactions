# ── Web / API ─────────────────────────────────────────────────────────────────
fastapi==0.116.1
uvicorn==0.35.0
pydantic==2.11.7
PyYAML==6.0.3
python-multipart==0.0.20

# ── OpenVINO runtime + GenAI (verified: Arrow Lake iGPU, kernel 6.17, NEO 26.18) ──
openvino==2026.1.0
openvino-genai==2026.1.0.0
openvino-tokenizers==2026.1.0.0

# ── Model export (optimum-cli export openvino) ────────────────────────────────
# PINNED: 1.27.0/2.1.0 produce a 0-byte openvino_model.xml with int8 compression.
# 1.24.0/1.26.1 is the verified working combination for this OV stack.
optimum==1.26.1
optimum-intel[openvino]==1.24.0
nncf==2.19.0

# ── HuggingFace / ML stack ───────────────────────────────────────────────────
torch==2.8.0
transformers==4.52.4
tokenizers==0.21.4
huggingface_hub==0.34.4
safetensors==0.6.2
numpy==1.26.4

# ── Embeddings ───────────────────────────────────────────────────────────────
sentence-transformers==3.0.1

# ── RAG / vector store ───────────────────────────────────────────────────────
langchain-chroma>=0.2.5
langchain-core>=0.3.74
chromadb>=1.0.20
